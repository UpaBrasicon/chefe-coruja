# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Credenciais — NUNCA versionar
.env.local

# VIDaaS spike — tokens e saídas NUNCA versionar
spikes/vidaas/saida/
spikes/vidaas/*.token
spikes/vidaas/.vidaas-env

# Strix — artefatos de varredura local (nunca versionar; resumos em docs/seguranca/)
strix_runs/

# deepseek-harness — clone externo do harness (tem .git próprio; nunca versionar)
deepseek-harness/

# Dados brutos de terminologia — fontes públicas regeneráveis (~118MB), ver LEIA-ME.txt
data/terminologia/*
!data/terminologia/LEIA-ME.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
