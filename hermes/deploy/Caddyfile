# ─────────────────────────────────────────────────────────────────────────────
# HERMES — Caddyfile (proxy reverso + HTTPS automático Let's Encrypt)
# MODO ATUAL: acesso por IP (HTTP) — para validar antes do domínio.
# Quando o DNS apontar (hermes.chefecoruja.com.br → IP), troque para o bloco
# com domínio (abaixo, comentado) que o Caddy emite HTTPS automático.
# ─────────────────────────────────────────────────────────────────────────────

:80 {
	# /webhook precisa do corpo BRUTO para validação HMAC — o Caddy repassa
	# o body sem alterações (não usamos compressão aqui).
	reverse_proxy app:3000

	# Sem log de acesso por padrão (descomente para debug):
	# log {
	#   output file /data/caddy-access.log
	# }
}

# ── Produção final (após apontar o DNS) ───────────────────────────────────────
# hermes.chefecoruja.com.br {
# 	reverse_proxy app:3000
# }
